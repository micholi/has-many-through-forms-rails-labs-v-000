<h1><%= @post.title %></h1>
<p><%= @post.content %></p>
<% @post.comments.each do |comment| %>
  <%= comment.content %>
<% end %>
<% @post.users.uniq.each do |user| %>
  <%= link_to user.username, user_path(user) %>
<% end %>
